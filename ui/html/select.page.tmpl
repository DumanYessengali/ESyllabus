{{template "base" .}}
{{define "title"}}Home{{end}}
{{define "main"}}

<table class="cinereousTable">
    <thead>
        <tr>
            <th>Название силлабуса</th>
            <th> </th>
        </tr>
    </thead>
    <tbody>
     {{range .Syllabus}}


        <tr>
            <td>
                #{{.Title}}
            </td>
            <td>
                #{{.ID}}
            </td>
            <td>
               {{.Goals}}
            </td>
        </tr>
        {{end}}
        </tbody>
</table><br>
          <a href="https://localhost:4000/admin/create" class="myButton">&nbsp;Create </a>
{{end}}
